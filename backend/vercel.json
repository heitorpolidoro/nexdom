{
  "buildCommand": "python -m alembic upgrade head",
  "builds": [
    {
      "src": "index.py",
      "use": "@vercel/python"
    }
  ],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "index.py"
    }
  ],
  "ignoreCommand": "if [ \"$VERCEL_ENV\" = \"production\" ]; then git describe --exact-match --tags HEAD 2>/dev/null | grep -qE '^v[0-9]' || exit 0; fi; exit 1"
}
